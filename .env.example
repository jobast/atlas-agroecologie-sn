# Copy to .env and fill in real values. .env is gitignored.

# --- Postgres ---
POSTGRES_DB=atlas
POSTGRES_USER=atlas
POSTGRES_PASSWORD=change-me

# --- App ---
JWT_SECRET=change-me-to-a-long-random-string
ADMIN_EMAIL=joanbastide@gmail.com
ALLOWED_ORIGINS=https://atlas.creates.ngo
FRONTEND_URL=https://atlas.creates.ngo

# --- SMTP (Infomaniak, Mailgun, Brevo, etc.) ---
SMTP_HOST=mail.infomaniak.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=

# --- Public hostname (used by Traefik labels) ---
ATLAS_HOST=atlas.creates.ngo
