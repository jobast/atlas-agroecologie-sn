node_modules/
.env
.env.*
!.env.production
!.env.example
!server/.env.production
!client/.env.production
npm-debug.log*
.DS_Store
# Frontend build (prod only)
index.html
assets/

# Server-local
client/package-lock.json

# User uploads (photos) - never commit
server/uploads/

# Local DB dumps / backup artefacts
exports/
backups/
*.sql.gz
*.tar.gz
